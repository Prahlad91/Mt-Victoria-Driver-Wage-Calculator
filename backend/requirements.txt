fastapi==0.115.0
uvicorn[standard]==0.30.1
pydantic==2.7.1
python-multipart==0.0.9
pdfplumber==0.11.0
openpyxl==3.1.2
reportlab==4.2.0
PyYAML==6.0.1
pytest==8.2.0
httpx==0.27.0
Pillow>=10.0.0
pytesseract>=0.3.10
# v3.22 — server-side persistence for parsed roster/schedule/chart payloads.
# asyncpg is async-native and pairs well with FastAPI. Falls back to a no-op
# storage layer when DATABASE_URL is unset (local dev / first-boot).
asyncpg>=0.29.0
# v3.31: JWT issuance + verification for driver authentication.
# PyJWT is the canonical Python library; 2.x has the modern API.
PyJWT>=2.9.0
