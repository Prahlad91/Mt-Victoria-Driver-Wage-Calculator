{
  "version": 2,
  "buildCommand": "cd frontend && npm install && npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "https://mt-victoria-wage-calc-api.onrender.com/api/$1" },
    { "source": "/(.*)",     "destination": "/index.html" }
  ]
}
